# Поддержка документации

## 📋 Содержание
1. [Принципы поддержки](#принципы-поддержки)
2. [Процесс обновления](#процесс-обновления)
3. [Чек-листы](#чек-листы)
4. [Автоматизация](#автоматизация)
5. [Контроль качества](#контроль-качества)

---

## Принципы поддержки

### 1. Всегда актуальная документация
- **Документация = код** - документация обновляется вместе с кодом
- **Никаких устаревших примеров** - все примеры должны работать
- **Проверка работоспособности** - каждый пример тестируется

### 2. Единый стиль
- **Консистентное форматирование** - одинаковый стиль во всех файлах
- **Структурированность** - четкая иерархия разделов
- **Читаемость** - понятный язык и примеры

### 3. Полнота информации
- **Все API методы** - каждый метод должен быть описан
- **Примеры использования** - для каждого API метода
- **Обработка ошибок** - как обрабатывать ошибки

---

## Процесс обновления

### При изменении CSS классов

#### 1. Обновить CSS_CLASSES.md
```markdown
### `.new-class`
**Назначение**: Описание назначения класса
```css
.new-class {
  /* CSS свойства */
}
```

**Пример использования**:
```html
<div class="new-class">Контент</div>
```
```

#### 2. Обновить README.md
- Добавить класс в раздел "CSS Design System"
- Обновить примеры использования
- Проверить актуальность

#### 3. Проверить работоспособность
- Запустить проект
- Проверить, что класс работает
- Убедиться, что примеры корректны

### При изменении JavaScript API

#### 1. Обновить JAVASCRIPT_API.md
```markdown
#### `newFunction()`
**Назначение**: Описание назначения функции
```javascript
newFunction(parameter) {
  // код функции
}
```

**Пример использования**:
```javascript
newFunction('test');
```
```

#### 2. Обновить JSDoc комментарии
```javascript
/**
 * Описание функции
 * @param {string} parameter - Описание параметра
 * @returns {void}
 */
function newFunction(parameter) {
  // код функции
}
```

#### 3. Обновить README.md
- Добавить функцию в раздел "JavaScript API"
- Обновить примеры использования
- Проверить актуальность

### При изменении структуры проекта

#### 1. Обновить README.md
- Обновить раздел "Структура проекта"
- Проверить актуальность путей
- Обновить описание файлов

#### 2. Обновить SPEC.md
- Обновить раздел "Структура проекта"
- Проверить актуальность подключений
- Обновить описание модулей

#### 3. Проверить все ссылки
- Убедиться, что все пути корректны
- Проверить, что все файлы существуют
- Обновить устаревшие ссылки

---

## Чек-листы

### Чек-лист обновления CSS классов

- [ ] Класс добавлен в CSS_CLASSES.md
- [ ] Указано назначение класса
- [ ] Добавлен пример CSS кода
- [ ] Добавлен пример HTML использования
- [ ] Класс добавлен в README.md
- [ ] Примеры протестированы в браузере
- [ ] Нет устаревших ссылок

### Чек-лист обновления JavaScript API

- [ ] Функция добавлена в JAVASCRIPT_API.md
- [ ] Указано назначение функции
- [ ] Добавлены параметры и возвращаемые значения
- [ ] Добавлен пример использования
- [ ] Обновлены JSDoc комментарии
- [ ] Функция добавлена в README.md
- [ ] Примеры протестированы в браузере
- [ ] Нет устаревших ссылок

### Чек-лист обновления структуры проекта

- [ ] Структура обновлена в README.md
- [ ] Структура обновлена в SPEC.md
- [ ] Все пути проверены
- [ ] Все файлы существуют
- [ ] Нет устаревших ссылок
- [ ] Порядок подключения актуален

### Чек-лист общего обновления

- [ ] Все файлы документации обновлены
- [ ] Все примеры протестированы
- [ ] Нет устаревших ссылок
- [ ] Стиль документации консистентен
- [ ] Информация полная и актуальная

---

## Автоматизация

### Скрипт проверки документации
```bash
#!/bin/bash
# check-docs.sh

echo "Проверка документации..."

# Проверка существования файлов
files=("README.md" "CSS_CLASSES.md" "JAVASCRIPT_API.md" "SPEC.md")
for file in "${files[@]}"; do
  if [ ! -f "$file" ]; then
    echo "❌ Файл $file не найден"
    exit 1
  else
    echo "✅ Файл $file существует"
  fi
done

# Проверка ссылок в README.md
echo "Проверка ссылок в README.md..."
grep -o '\[.*\](.*)' README.md | while read -r link; do
  if [[ $link == *"("*")"* ]]; then
    url=$(echo "$link" | sed 's/.*(\(.*\)).*/\1/')
    if [[ $url == http* ]]; then
      if ! curl -s --head "$url" | head -n 1 | grep -q "200 OK"; then
        echo "❌ Ссылка $url недоступна"
      else
        echo "✅ Ссылка $url доступна"
      fi
    fi
  fi
done

echo "Проверка завершена"
```

### Скрипт обновления версии
```bash
#!/bin/bash
# update-version.sh

VERSION=$1
if [ -z "$VERSION" ]; then
  echo "Использование: ./update-version.sh 1.0.1"
  exit 1
fi

echo "Обновление версии до $VERSION..."

# Обновление версии в README.md
sed -i '' "s/Версия.*/Версия: $VERSION/" README.md

# Обновление версии в CSS_CLASSES.md
sed -i '' "s/Версия.*/Версия: $VERSION/" CSS_CLASSES.md

# Обновление версии в JAVASCRIPT_API.md
sed -i '' "s/Версия.*/Версия: $VERSION/" JAVASCRIPT_API.md

# Обновление даты
DATE=$(date +%Y-%m-%d)
sed -i '' "s/Последнее обновление.*/Последнее обновление: $DATE/" README.md
sed -i '' "s/Последнее обновление.*/Последнее обновление: $DATE/" CSS_CLASSES.md
sed -i '' "s/Последнее обновление.*/Последнее обновление: $DATE/" JAVASCRIPT_API.md

echo "Версия обновлена до $VERSION"
```

---

## Контроль качества

### Проверка перед коммитом
```bash
#!/bin/bash
# pre-commit-check.sh

echo "Проверка документации перед коммитом..."

# Запуск проверки документации
./check-docs.sh

if [ $? -eq 0 ]; then
  echo "✅ Документация в порядке"
  exit 0
else
  echo "❌ Документация требует исправлений"
  exit 1
fi
```

### Проверка после изменений
```bash
#!/bin/bash
# post-change-check.sh

echo "Проверка документации после изменений..."

# Проверка, что все файлы обновлены
git status --porcelain | grep -E "\.(md|js|css)$" | while read -r file; do
  echo "Проверка файла: $file"
  
  # Проверка синтаксиса markdown
  if [[ $file == *.md ]]; then
    if command -v markdownlint &> /dev/null; then
      markdownlint "$file"
    fi
  fi
  
  # Проверка синтаксиса JavaScript
  if [[ $file == *.js ]]; then
    if command -v jshint &> /dev/null; then
      jshint "$file"
    fi
  fi
done

echo "Проверка завершена"
```

---

## Шаблоны

### Шаблон для нового CSS класса
```markdown
### `.class-name`
**Назначение**: Краткое описание назначения класса
```css
.class-name {
  /* CSS свойства */
}
```

**Пример использования**:
```html
<div class="class-name">Контент</div>
```

**Связанные классы**:
- `.related-class` - описание связи
```

### Шаблон для нового JavaScript API
```markdown
#### `functionName(parameter)`
**Назначение**: Описание назначения функции
```javascript
functionName(parameter) {
  // код функции
}
```

**Параметры**:
- `parameter` (string) - описание параметра

**Возвращаемое значение**:
- `void` - описание возвращаемого значения

**Пример использования**:
```javascript
functionName('test');
```
```

### Шаблон для нового модуля
```markdown
## ModuleName

### Описание
Краткое описание модуля и его назначения.

### API

#### `functionName()`
**Назначение**: Описание назначения функции
```javascript
functionName() {
  // код функции
}
```

### Функциональность
- **Функция 1** - описание
- **Функция 2** - описание

### Примеры использования
```javascript
// Пример использования
```
```

---

## Мониторинг

### Отслеживание изменений
```bash
# Отслеживание изменений в документации
git log --oneline --grep="docs" --grep="documentation" --grep="README" --grep="API"

# Отслеживание изменений в CSS классах
git log --oneline --grep="css" --grep="class" --grep="style"

# Отслеживание изменений в JavaScript API
git log --oneline --grep="js" --grep="api" --grep="function"
```

### Статистика документации
```bash
# Подсчет строк в документации
find . -name "*.md" -exec wc -l {} + | tail -1

# Подсчет примеров кода
grep -r "```" . --include="*.md" | wc -l

# Подсчет ссылок
grep -r "\[.*\](.*)" . --include="*.md" | wc -l
```

---

## Резервное копирование

### Создание бэкапа документации
```bash
#!/bin/bash
# backup-docs.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="docs_backup_$DATE"

echo "Создание резервной копии документации..."

mkdir -p "$BACKUP_DIR"

# Копирование файлов документации
cp README.md "$BACKUP_DIR/"
cp CSS_CLASSES.md "$BACKUP_DIR/"
cp JAVASCRIPT_API.md "$BACKUP_DIR/"
cp SPEC.md "$BACKUP_DIR/"
cp DOCUMENTATION_MAINTENANCE.md "$BACKUP_DIR/"

# Создание архива
tar -czf "docs_backup_$DATE.tar.gz" "$BACKUP_DIR"
rm -rf "$BACKUP_DIR"

echo "Резервная копия создана: docs_backup_$DATE.tar.gz"
```

---

**Последнее обновление**: 2025-09-21  
**Версия**: 1.0.0
