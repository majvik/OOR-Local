# JavaScript API - Полная документация

## 📋 Содержание
1. [Обзор API](#обзор-api)
2. [ErrorHandler](#errorhandler)
3. [Cursor (MouseFollower)](#cursor-mousefollower)
4. [Navigation](#navigation)
5. [Video](#video)
6. [Preloader](#preloader)
7. [Scrollbar](#scrollbar)
8. [MerchImages](#merchimages)
9. [Глобальные утилиты](#глобальные-утилиты)

---

## Обзор API

### Основные модули
```javascript
// Глобальный объект OOR
window.OOR = {
  errorHandler: ErrorHandler,
  safeExecute: Function,
  safeExecuteAsync: Function,
  checkAPI: Function,
  loadScript: Function
};
```

### Порядок инициализации
1. **ErrorHandler** - инициализируется первым
2. **Preloader** - загружается после ErrorHandler
3. **Navigation** - инициализируется после DOM ready
4. **Video** - инициализируется после Navigation
5. **Cursor** - инициализируется после всех модулей

---

## ErrorHandler

### Описание
Централизованная система обработки ошибок с логированием и graceful degradation.

### API

#### `ErrorHandler.init()`
**Назначение**: Инициализация обработчика ошибок
```javascript
window.OOR.errorHandler.init();
```

#### `ErrorHandler.handleError(error, context)`
**Назначение**: Обработка ошибки
```javascript
window.OOR.errorHandler.handleError(new Error('Test error'), 'test-context');
```

#### `ErrorHandler.shouldIgnoreError(error)`
**Назначение**: Проверка, нужно ли игнорировать ошибку
```javascript
const shouldIgnore = window.OOR.errorHandler.shouldIgnoreError(error);
```

#### `ErrorHandler.logError(error, context)`
**Назначение**: Логирование ошибки
```javascript
window.OOR.errorHandler.logError(error, 'module-name');
```

#### `ErrorHandler.showUserError(message)`
**Назначение**: Показ ошибки пользователю
```javascript
window.OOR.errorHandler.showUserError('Что-то пошло не так');
```

#### `ErrorHandler.sendToAnalytics(error, context)`
**Назначение**: Отправка ошибки в аналитику
```javascript
window.OOR.errorHandler.sendToAnalytics(error, 'user-action');
```

### Примеры использования
```javascript
// Безопасное выполнение кода
window.OOR.safeExecute(() => {
  // код, который может упасть
  const element = document.querySelector('.non-existent');
  element.classList.add('active');
});

// Асинхронное выполнение
window.OOR.safeExecuteAsync(async () => {
  const response = await fetch('/api/data');
  const data = await response.json();
  console.log(data);
});
```

---

## Cursor (MouseFollower)

### Описание
Кастомный курсор на основе библиотеки MouseFollower с поддержкой текста, изображений и видео.

### Автоматическая инициализация
Курсор инициализируется автоматически при загрузке страницы.

### HTML атрибуты
```html
<!-- Текстовый курсор -->
<div data-cursor="text" data-cursor-text="Кликни сюда">
  Элемент с текстовым курсором
</div>

<!-- Изображение курсора -->
<div data-cursor="img" data-cursor-img="/path/to/image.jpg">
  Элемент с изображением курсора
</div>

<!-- Видео курсор -->
<div data-cursor="video" data-cursor-video="/path/to/video.mp4">
  Элемент с видео курсором
</div>
```

### CSS классы
```css
/* Базовые стили курсора */
.mf-cursor {
  /* Стили основного курсора */
}

.mf-cursor-text {
  /* Стили текстового курсора */
}

.mf-cursor-media {
  /* Стили медиа курсора */
}
```

### Настройки
```javascript
// Настройки MouseFollower (в cursor.js)
const cursor = new MouseFollower({
  speed: 0.4,
  showTimeout: 0,
  hideTimeout: 50,
  // другие настройки...
});
```

---

## Navigation

### Описание
Модуль для управления навигацией с поддержкой активных состояний.

### API

#### `initNavigation()`
**Назначение**: Инициализация навигации
```javascript
// Автоматически вызывается при загрузке страницы
```

### Функциональность
- **Активные состояния** - автоматическое определение активной страницы
- **WordPress совместимость** - поддержка классов `.current-menu-item`, `.current_page_item`
- **Плавные переходы** - анимации при смене активного состояния

### CSS классы
```css
/* Активное состояние ссылки */
.oor-nav-link--active {
  /* стили активного состояния */
}

/* WordPress классы */
.current-menu-item .oor-nav-link,
.current_page_item .oor-nav-link,
.current-menu-ancestor .oor-nav-link {
  /* стили активного состояния */
}
```

---

## Video

### Описание
Модуль для управления видео с поддержкой полноэкранного режима.

### API

#### `initHeroVideo()`
**Назначение**: Инициализация герой видео
```javascript
// Автоматически вызывается при загрузке страницы
```

#### `initFullscreenVideo()`
**Назначение**: Инициализация полноэкранного видео
```javascript
// Автоматически вызывается при загрузке страницы
```

### Функциональность
- **Автовоспроизведение** - видео запускается автоматически
- **Полноэкранный режим** - поддержка полноэкранного просмотра
- **Обработка ошибок** - graceful handling ошибок воспроизведения
- **Адаптивность** - видео адаптируется под размер контейнера

### HTML структура
```html
<div class="oor-media-cover">
  <video autoplay muted loop>
    <source src="/path/to/video.mp4" type="video/mp4">
  </video>
</div>
```

---

## Preloader

### Описание
Модуль для управления прелоадером страницы.

### API

#### `initPreloader()`
**Назначение**: Инициализация прелоадера
```javascript
// Автоматически вызывается при загрузке страницы
```

### Функциональность
- **Автоматическое скрытие** - прелоадер скрывается после загрузки
- **Анимации** - плавные переходы при скрытии
- **Обработка ошибок** - прелоадер скрывается даже при ошибках

### HTML структура
```html
<div class="preloader">
  <div class="preloader-content">
    <!-- Содержимое прелоадера -->
  </div>
</div>
```

---

## Scrollbar

### Описание
Модуль для кастомного скроллбара.

### API

#### `updateScrollbar()`
**Назначение**: Обновление кастомного скроллбара
```javascript
// Автоматически вызывается при скролле и изменении размера окна
```

### Функциональность
- **Кастомный дизайн** - стилизованный скроллбар
- **Автоматическое обновление** - обновляется при изменении контента
- **Кроссбраузерность** - работает во всех современных браузерах

### CSS классы
```css
.custom-scrollbar {
  /* Стили кастомного скроллбара */
}

.custom-scrollbar-track {
  /* Стили трека скроллбара */
}

.custom-scrollbar-thumb {
  /* Стили ползунка скроллбара */
}
```

---

## MerchImages

### Описание
Модуль для динамического изменения размеров изображений мерча.

### API

#### `initMerchImages()`
**Назначение**: Инициализация изображений мерча
```javascript
// Автоматически вызывается при загрузке страницы
```

### Функциональность
- **Динамическое изменение размеров** - изображения адаптируются под контейнер
- **Оптимизация производительности** - эффективное управление DOM
- **Обработка ошибок** - graceful handling ошибок загрузки изображений

### HTML структура
```html
<div class="oor-merch-image-item">
  <img src="/path/to/image.jpg" alt="Merch item">
</div>
```

---

## Глобальные утилиты

### `window.OOR.safeExecute(fn)`
**Назначение**: Безопасное выполнение функции
```javascript
window.OOR.safeExecute(() => {
  // код, который может упасть
  console.log('Выполняется безопасно');
});
```

### `window.OOR.safeExecuteAsync(fn)`
**Назначение**: Безопасное выполнение асинхронной функции
```javascript
window.OOR.safeExecuteAsync(async () => {
  const response = await fetch('/api/data');
  const data = await response.json();
  console.log(data);
});
```

### `window.OOR.checkAPI(api)`
**Назначение**: Проверка доступности API
```javascript
window.OOR.checkAPI('gsap').then(available => {
  if (available) {
    console.log('GSAP доступен');
  }
});
```

### `window.OOR.loadScript(src)`
**Назначение**: Динамическая загрузка скрипта
```javascript
window.OOR.loadScript('/path/to/script.js').then(() => {
  console.log('Скрипт загружен');
});
```

---

## Примеры использования

### Базовое использование
```javascript
// Инициализация всех модулей происходит автоматически
// Дополнительная настройка не требуется
```

### Обработка ошибок
```javascript
// Безопасное выполнение кода
window.OOR.safeExecute(() => {
  const element = document.querySelector('.my-element');
  element.classList.add('active');
});

// Обработка ошибок вручную
try {
  // код, который может упасть
} catch (error) {
  window.OOR.errorHandler.handleError(error, 'my-module');
}
```

### Работа с курсором
```html
<!-- Добавление атрибутов курсора -->
<button data-cursor="text" data-cursor-text="Нажми меня">
  Кнопка
</button>

<div data-cursor="img" data-cursor-img="/path/to/image.jpg">
  Элемент с изображением курсора
</div>
```

### Работа с видео
```html
<!-- Герой видео -->
<div class="oor-hero">
  <div class="oor-media-cover">
    <video autoplay muted loop>
      <source src="/path/to/hero-video.mp4" type="video/mp4">
    </video>
  </div>
</div>
```

---

## Отладка

### Включение отладки
```javascript
// В консоли браузера
window.OOR.errorHandler.debug = true;
```

### Проверка состояния модулей
```javascript
// Проверка инициализации ErrorHandler
console.log(window.OOR.errorHandler.initialized);

// Проверка доступности GSAP
window.OOR.checkAPI('gsap').then(available => {
  console.log('GSAP доступен:', available);
});
```

### Логирование
```javascript
// Все ошибки автоматически логируются в консоль
// с префиксом [OOR Error]
```

---

## Обновление документации

### При добавлении нового API
1. Добавить описание в этот файл
2. Указать примеры использования
3. Обновить README.md
4. Проверить актуальность

### При изменении существующего API
1. Обновить описание в этом файле
2. Обновить примеры использования
3. Проверить совместимость
4. Обновить README.md

---

**Последнее обновление**: 2025-09-21  
**Версия**: 1.0.0
