# Руководство по установке темы WordPress

> **Версия:** 2.0.0  
> **Дата:** 2025-01-15

## Требования

- WordPress 6.0+ (последняя стабильная версия)
- PHP 8.0+
- MySQL 5.7+ или MariaDB 10.3+
- Плагин ACF Pro (обязательно)
- Docker и Docker Compose (для локальной разработки)
- VPS хостинг (для продакшена)

---

## 🐳 Установка через Docker (рекомендуется для разработки)

### Преимущества Docker

- ✅ Быстрая настройка окружения одной командой
- ✅ Изоляция от основной системы
- ✅ Воспроизводимое окружение
- ✅ Легкое переключение версий PHP/MySQL
- ✅ Не требует установки PHP/MySQL на хост-систему

### 1. Установка Docker

**macOS:**
```bash
# Установите Docker Desktop для Mac
# https://www.docker.com/products/docker-desktop
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install docker.io docker-compose

# Запустите Docker
sudo systemctl start docker
sudo systemctl enable docker
```

**Windows:**
```bash
# Установите Docker Desktop для Windows
# https://www.docker.com/products/docker-desktop
```

### 2. Подготовка проекта

1. Убедитесь, что Docker запущен
2. В корне проекта уже есть `docker-compose.yml`
3. Создайте необходимые директории:
   ```bash
   mkdir -p wp-content/themes/oor-theme
   mkdir -p wordpress-uploads
   ```

### 3. Запуск Docker контейнеров

```bash
# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Просмотр логов
docker-compose logs -f wordpress
```

**Доступ:**
- WordPress: http://localhost:8080
- phpMyAdmin: http://localhost:8081

### 4. Первоначальная настройка WordPress

1. Откройте http://localhost:8080
2. Выберите язык
3. Заполните данные:
   - **Название сайта:** Out of Records
   - **Имя пользователя:** admin (или другое)
   - **Пароль:** создайте надежный пароль
   - **Email:** ваш email
4. Нажмите "Установить WordPress"

**Данные для подключения к БД (если понадобятся):**
- Хост БД: `db`
- Имя БД: `wordpress`
- Пользователь: `wordpress`
- Пароль: `wordpress`

### 5. Установка темы в Docker

1. Скопируйте файлы темы в `wp-content/themes/oor-theme/`:
   ```bash
   # Структура должна быть:
   wp-content/themes/oor-theme/
   ├── style.css
   ├── functions.php
   ├── header.php
   ├── footer.php
   ├── assets/ (из src/)
   ├── public/
   └── inc/
   ```

2. Или используйте volume mount (уже настроено в docker-compose.yml):
   - Файлы в `./wp-content` автоматически доступны в контейнере

3. Активируйте тему в WordPress админ-панели: **Внешний вид → Темы**

### 6. Полезные Docker команды

```bash
# Остановить контейнеры
docker-compose stop

# Запустить контейнеры
docker-compose start

# Перезапустить контейнеры
docker-compose restart

# Остановить и удалить контейнеры (БД сохранится в volume)
docker-compose down

# Остановить и удалить все, включая volumes (удалит БД!)
docker-compose down -v

# Войти в контейнер WordPress
docker-compose exec wordpress bash

# Войти в контейнер MySQL
docker-compose exec db mysql -u wordpress -pwordpress wordpress

# Просмотр логов
docker-compose logs wordpress
docker-compose logs db

# Пересоздать контейнеры
docker-compose up -d --force-recreate
```

### 7. Работа с файлами

**Копирование файлов в контейнер:**
```bash
# Копировать файл в контейнер
docker cp local-file.php oor-wordpress:/var/www/html/

# Копировать из контейнера
docker cp oor-wordpress:/var/www/html/file.php ./
```

**Изменение прав доступа:**
```bash
# Войти в контейнер
docker-compose exec wordpress bash

# Изменить права
chown -R www-data:www-data /var/www/html/wp-content
chmod -R 755 /var/www/html/wp-content
```

### 8. Бэкапы в Docker

```bash
# Бэкап базы данных
docker-compose exec db mysqldump -u wordpress -pwordpress wordpress > backup.sql

# Восстановление базы данных
docker-compose exec -T db mysql -u wordpress -pwordpress wordpress < backup.sql

# Бэкап wp-content
docker cp oor-wordpress:/var/www/html/wp-content ./wp-content-backup
```

### 9. Переменные окружения

Для изменения настроек отредактируйте `docker-compose.yml`:

```yaml
environment:
  WORDPRESS_DB_HOST: db
  WORDPRESS_DB_USER: wordpress
  WORDPRESS_DB_PASSWORD: wordpress  # Измените на свой пароль
  WORDPRESS_DB_NAME: wordpress
  WORDPRESS_DEBUG: 1  # 0 для продакшена
```

После изменений перезапустите:
```bash
docker-compose down
docker-compose up -d
```

---

## 📦 Установка без Docker (традиционный способ)

### 1. Подготовка WordPress

1. Установите WordPress на сервер
2. Убедитесь, что SSL сертификат настроен (HTTPS обязательно)
3. Настройте автоматические бэкапы

### 2. Установка ACF Pro

1. Установите плагин ACF Pro через админ-панель WordPress
2. Активируйте плагин
3. Убедитесь, что плагин работает корректно

### 3. Установка темы

1. Скопируйте содержимое проекта в `/wp-content/themes/oor-theme/`
2. Структура должна быть:
   ```
   /wp-content/themes/oor-theme/
   ├── style.css
   ├── functions.php
   ├── index.php
   ├── header.php
   ├── footer.php
   ├── front-page.php
   ├── page.php
   ├── single-artist.php
   ├── archive-event.php
   ├── assets/
   │   ├── css/
   │   └── js/
   ├── public/
   │   ├── assets/
   │   └── fonts/
   └── inc/
   ```

3. Активируйте тему в WordPress админ-панели: **Внешний вид → Темы**

### 4. Настройка структуры

#### Создание страниц

Создайте следующие страницы в WordPress:
- Главная (Home) - установите как главную страницу
- Манифест
- Артисты
- Студия
- Услуги
- DAWGS
- Talk-show
- События
- Мерч (для WooCommerce)
- Контакты

#### Настройка меню

1. Перейдите в **Внешний вид → Меню**
2. Создайте главное меню
3. Добавьте все страницы в меню
4. Назначьте меню в позицию "Главное меню"

### 5. Настройка Custom Post Types

Custom Post Types регистрируются автоматически через `functions.php`:
- **Артисты** (`artist`) - появится в меню админ-панели
- **События** (`event`) - появится в меню админ-панели

### 6. Настройка ACF полей

1. Перейдите в **Произвольные поля → Группы полей**
2. Импортируйте или создайте Field Groups согласно [ACF-FIELDS-GUIDE.md](ACF-FIELDS-GUIDE.md)

**Важно:** Убедитесь, что Location Rules настроены правильно:
- Главная страница: `Page Template is equal to Главная`
- Страница артиста: `Post Type is equal to artist`
- Страница события: `Post Type is equal to event`
- DAWGS: `Page Template is equal to DAWGS`
- Talk-show: `Page Template is equal to Talk-show`

### 7. Настройка путей

Убедитесь, что в `functions.php` правильно настроены пути:
- `get_template_directory_uri()` используется для всех ресурсов
- JavaScript использует `wp_localize_script()` для получения путей

### 8. Настройка кэширования

Для VPS рекомендуется:
1. Установить Memcached или Redis
2. Установить плагин Object Cache (WP Redis или Memcached)
3. Настроить Page Cache (WP Rocket или W3 Total Cache)

### 9. Настройка аналитики

Добавьте коды Google Analytics и Яндекс.Метрики в `functions.php`:
```php
// См. раздел "Аналитика" в OPTIMIZATION.md
```

### 10. Тестирование

1. Проверьте все страницы на корректное отображение
2. Проверьте работу ACF полей
3. Проверьте добавление/редактирование артистов и событий
4. Проверьте body-классы на всех страницах
5. Проверьте работу всех JavaScript функций

## Troubleshooting

### Проблема: Ресурсы не загружаются (404)

**Решение:**
- Проверьте, что пути используют `get_template_directory_uri()`
- Очистите кэш браузера и WordPress
- Проверьте права доступа к файлам

### Проблема: Стили не применяются

**Решение:**
- Проверьте порядок загрузки CSS в `inc/enqueue.php`
- Убедитесь, что все CSS файлы на месте
- Проверьте body-классы на страницах

### Проблема: ACF поля не отображаются

**Решение:**
- Проверьте Location Rules в ACF
- Убедитесь, что правильный шаблон страницы выбран
- Проверьте, что поля добавлены в правильную группу

### Проблема: JavaScript не работает

**Решение:**
- Проверьте консоль браузера на ошибки
- Убедитесь, что все скрипты загружаются
- Проверьте зависимости скриптов (GSAP, Lenis)

## Troubleshooting Docker

### Проблема: Порт 8080 уже занят

**Решение:**
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8082:80"  # Вместо 8080
```

### Проблема: Файлы не обновляются в контейнере

**Решение:**
```bash
# Перезапустите контейнер
docker-compose restart wordpress

# Или пересоздайте
docker-compose up -d --force-recreate wordpress
```

### Проблема: Ошибки прав доступа

**Решение:**
```bash
# Войти в контейнер
docker-compose exec wordpress bash

# Изменить права
chown -R www-data:www-data /var/www/html/wp-content
chmod -R 755 /var/www/html/wp-content
```

### Проблема: База данных не сохраняется

**Решение:**
Убедитесь, что volume `mysql-data` создан:
```bash
docker volume ls | grep mysql-data
```

Если volume отсутствует, пересоздайте контейнеры:
```bash
docker-compose down
docker-compose up -d
```

## Дополнительные ресурсы

- [План оптимизации](OPTIMIZATION.md) - детальный план оптимизации
- [Руководство по ACF полям](ACF-FIELDS-GUIDE.md) - как редактировать контент
- [Техническая спецификация](SPEC.md) - технические детали проекта
- [Руководство по работе с агентом Cursor](CURSOR-AGENT-GUIDE.md) - работа с AI-ассистентом

---

**Поддержка:** При возникновении проблем проверьте документацию или обратитесь к разработчику.
